<h1>Hello</h1>
<button class="btn btn-success" onclick="smth()">click</button>

<div class="hint" style="display: none">
      Hello to first time visitors!
</div>


<div class="container-fluid">
	<div class="row">
		<% @product.each do |var| %>
			<div class="col-md-3"><img src="<%= var.path_to_image_sm %>">
			<p><%= var.title %></p><br>
			<p><%= var.price %></p><br>
			<p><a href="/product/<%= var.id %>">link</a></p>
			</div>
		<% end %>
	</div>
</div>


